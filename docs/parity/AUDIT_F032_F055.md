# Parity audit — F032–F055 (context and input, permissions and safety)

Audited against extension 0.64.4 at commit `454b34d`. "Present is not wired": a
module with no callers is scaffolding, not SHIPPED.

| ID   | Feature                                             | Class                                 | Evidence                                                                                                                                                                                         | Gap                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---- | --------------------------------------------------- | ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F032 | @-mentions with fuzzy matching                      | SHIPPED in 0.98.0, was MISSING        | `src/core/mention-match.ts`, `src/services/mention-suggestion-service.ts`, `src/infrastructure/vscode-mention-index.ts`, `media/chat.js`                                                         | Subsequence matching ranked by where the characters landed, with folder entries so a mention can narrow by directory first. The extension owns the matching and returns the span the choice replaces, so the composer never re-derives the parser. `@path` and `@path:L-L` both reach `referencedRanges`, which now shares one guarded read for both spellings. Sensitive paths are kept out of the index entirely and still excluded downstream with a receipt reason.                                                                                                                                                                                                                                                                                 |
| F033 | Line-range references                               | PARTIAL, narrowed in 0.85.0           | `src/core/file-range-reference.ts`, `src/core/context-collector.ts` (`ContextCandidate.startLine`/`endLine`), `src/services/workspace-context-service.ts` (`referencedRanges`, `selection`)      | Typing `path:L-L` in a message now pulls that exact range into context, and a selection's own coordinates are preserved through the receipt and into the `<workspace-file>` tag the model sees. Still absent: the discoverable, autocomplete affordance for the syntax (F032, deliberately sequenced after this) and stale-range UI — nothing yet re-hashes a referenced file to flag that it changed since collection.                                                                                                                                                                                                                                                                                                                                 |
| F034 | Selection visibility toggle                         | SHIPPED                               | `src/webview/chat-composer-markup.ts:77`, `media/chat.js:942`, `src/services/workspace-context-service.ts:104`                                                                                   | Modes are exclusive: selection cannot be included alongside file or workspace.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| F035 | Terminal references                                 | SHIPPED in 1.15.0, was PARTIAL        | `src/core/terminal-reference.ts`, `src/infrastructure/vscode-terminal-capture.ts`                                                                                                                | A terminal the user is already using can be referenced from the composer. VS Code exposes terminal contents only as a stream while a command runs — there is no buffer API — so output is captured as it happens and only the last command per terminal is kept. A terminal without shell integration is named and reported unreadable rather than attached empty. Output is redacted, tagged as terminal output rather than pasted as prose, and trimmed from the end because that is why a log gets referenced.                                                                                                                                                                                                                                       |
| F036 | Browser references and integration                  | PARTIAL                               | `src/infrastructure/playwright-browser-driver.ts:118`, `src/core/browser-operation.ts:113`                                                                                                       | `allowedOrigins` is hardcoded to the backend and frontend URLs at `src/services/runtime-studio-helpers.ts:61`; no composer attach of browser state.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| F037 | Shift-drag attachments                              | PARTIAL                               | `media/chat.js:2158`, `:2148`, `src/services/attachment-request-service.ts:98`                                                                                                                   | No modifier semantics, no reorder, no drop target for editor or explorer URIs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| F038 | PDF page-range reading                              | MISSING                               | `src/core/chat-attachment.ts:15`, `src/infrastructure/playwright-browser-driver.ts:299`                                                                                                          | PDFs still upload raw. The shared chokepoint F038 and F039 were required to land together now exists as `prepareAttachments`, so the page-range work extends one function rather than rewriting the image path. Extraction itself needs a PDF parser dependency, undecided.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| F039 | Image understanding                                 | PARTIAL, narrowed in 1.10.0           | `src/core/image-metadata.ts`, `src/core/attachment-preparation.ts`, `src/core/model-vision.ts`                                                                                                   | Three of the four gaps closed: EXIF and PNG text chunks are stripped on this machine before upload, a model with `supportsVision: false` is never sent an image, and images are budgeted per request with the overrunning one refused rather than the ones before it. Resizing is the one left, and it is honestly blocked: it needs a pixel decoder, which means a native dependency, which is a decision this program should not make silently. Oversized images are refused with a reason instead.                                                                                                                                                                                                                                                   |
| F040 | Context window indicator                            | SHIPPED in 1.11.0, was PARTIAL        | `src/core/context-budget.ts`, `media/chat.js`, `chat-composer-markup.ts`                                                                                                                         | Both remaining gaps closed. A quarter of the window is reserved for the response, bounded at 1024 and 32000, because a request that fills the window leaves the model no room to answer. The composer warns at 85% of what is left and again when the draft will not fit, and stays silent under automatic routing or an unknown window rather than warning about a limit nobody can act on. This also supplies the denominator ordering constraint 6 said F041 needs.                                                                                                                                                                                                                                                                                  |
| F041 | Automatic compaction and manual compact             | SHIPPED in 1.25.0, was PARTIAL        | `src/core/context-compaction.ts`, `src/services/compact-conversation-command.ts`                                                                                                                 | Manual compaction ships: the conversation summarizes itself with its own model and continues in a new thread seeded with the result, leaving the original intact in history. Completed in 1.25.0: `clawAI.autoCompact` chooses between nothing, an offer, and automatic summarizing, defaulting to the offer. Measuring and acting are separate decisions because a run in flight would split the record across two threads, a nearly-full conversation stays nearly full so it must be raised once rather than on every render, and an unknown capacity must never trigger. The panel reports the running total it alone knows; the host derives the capacity it alone owns. The automatic mode reuses the manual command and only skips its question. |
| F042 | Side questions / BTW                                | SHIPPED in 1.17.0, was MISSING        | `src/core/side-question.ts`, `src/services/side-question-thread.ts`                                                                                                                              | A prompt path that does not touch the transcript: a separate thread, archived at creation so it never reaches the history list, with the answer opening as a document rather than a message. Deliberately context-free, and the answer says so — giving it the conversation context would mean writing into the thread, which is exactly what a side question avoids. The reuse note suggested a variant of `send` skipping `recordThread`; that would have left the exchange in the conversation thread, which is the pollution rather than the fix.                                                                                                                                                                                                   |
| F043 | Extended thinking controls                          | SHIPPED in 1.20.0, narrowed           | `src/core/reasoning-visibility.ts`, `src/webview/chat-view-provider.ts`, `media/chat.js`                                                                                                         | Audited as a UI gap, found to be a leak: the panel derived its token count from `stream.delta`, so the host had already posted the model's private reasoning in full across the webview boundary. `postEvent` is the one door every stream event passes through, and it now drops the text and forwards only the size, guarding three field names rather than the one today's backend sends. The row is a `<details>` carrying step count and size, because a rising number cannot tell a long think from a stalled request. Still open: effort options gated on a declared reasoning capability, which no catalog field reports.                                                                                                                       |
| F044 | Voice dictation                                     | MISSING                               | `src/webview/chat-composer-markup.ts:24`                                                                                                                                                         | Nothing exists.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| F045 | Auto permission classifier                          | SHIPPED                               | `src/services/runtime-policy-v2-adapter.ts:33`, `src/core/policy-v2.ts:100`, `src/services/runtime-tool-router.ts:23`                                                                            | Rules are regex on operation name; no learned or configured override.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| F046 | Multiple permission modes                           | CONFLICT                              | `package.json:275`, `src/webview/chat-composer-markup.ts:74`, `src/services/configuration-service.ts:187`                                                                                        | `ENTERPRISE_LOCKED` is offered as a free user choice and can be left at will. It is genuinely stricter than ASK — `src/core/policy-v2.ts:73` hard-denies elevation, production and destructive effects in that mode — but nothing binds a user to it, so it advertises an organizational lock that does not exist.                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| F047 | Editable Plan mode document                         | SHIPPED in 0.96.0, was PARTIAL        | `src/core/plan-revision.ts`, `src/infrastructure/planning-tool-executor.ts`, `docs/PLAN_REVISIONS.md`                                                                                            | A Markdown export now carries the plan in a `clawai-plan-revision` block, because prose alone is lossy; `adopt` reads either format back and reports `new`/`unchanged`/`revised`; the revision is sha256 over the key-sorted plan, so reformatting invents nothing. Any operation may name the revision it read, and a superseded one is refused as stale. Editing prose around the block is deliberately not an edit to the plan.                                                                                                                                                                                                                                                                                                                      |
| F048 | Restore Plan mode on resume                         | SHIPPED in 0.97.0, was PARTIAL        | `src/core/agent-mode.ts`, `src/core/durable-run-journal.ts`, `src/services/runtime-studio-execution.ts`, `src/services/runtime-journal-tracker.ts`                                               | Found worse than audited: Runtime V2 never applied Plan mode to the prompt at all, so the model discovered the mode by having every edit refused by the policy rail. The mode is now applied at run start, recorded on the durable journal alongside the plan revision, and restored on resume tighten-only in both directions. The journal, not `ChatSessionDescriptor`, is the durable record — the registry is in-memory and does not survive a reload.                                                                                                                                                                                                                                                                                              |
| F049 | Granular permission rules                           | SHIPPED in 0.75.0                     | `src/core/policy-v2.ts`, `src/services/runtime-policy-v2-adapter.ts`, `docs/CLAWAI_FOLDER_SPEC.md`                                                                                               | `.clawai/policies/policy.json` takes rules matching tool, operation, path glob and command glob. A rule may only tighten: there is no `allow` outcome, because the file is workspace content. Not covered: domain rules, which wait on a network tool to have domains.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| F050 | Read deny rules                                     | SHIPPED in 0.65.0 and 0.75.0          | `src/core/workspace-path-policy.ts:64`, `src/core/policy-v2.ts`, `src/services/workspace-context-service.ts:29`                                                                                  | Each surface is now both defended and extendable, without a fourth mechanism: tools through the 0.75.0 policy rules, context collection through `.clawai/ignore`, attachments through the 0.65.0 name screen. Built-ins can be added to and never removed at all three. The residual is that attachments cannot be extended by project config, because the webview sends a filename with no workspace path for a glob to match.                                                                                                                                                                                                                                                                                                                         |
| F051 | Sandboxed shell                                     | PARTIAL                               | `src/infrastructure/bounded-command-runner.ts:33`, `:87`, `src/core/command-spec.ts:34`                                                                                                          | Bounded, not sandboxed: no OS sandbox profile, no network isolation, no filesystem jail, no capability probe.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| F052 | Managed organization settings                       | SHIPPED in 0.81.0-0.82.0, was BLOCKED | `src/core/policy-v2.ts`, `src/backend/organization-policy-client.ts`, `src/services/model-service.ts`, `src/core/organization-permission-floor.ts`, `apps/claw-agent-service/src/modules/fleet/` | Unblocked by building the backend contract that did not exist: an organization policy, intersected across memberships, fetched with the account data. Enforced at three points, each the one place that has the information: tool calls in the runtime evaluator (0.81.0), the model catalog including local models (0.82.0), and permission-mode selection via a tighten-only clamp checked before the Autonomous Scoped confirmation dialog (0.82.0). Tighten-only and unsigned throughout, for the reason project rules are.                                                                                                                                                                                                                         |
| F053 | Hard deny rules and trusted repositories or domains | PARTIAL, narrowed in 1.19.0           | `src/core/policy-v2.ts`, `src/services/runtime-policy-v2-adapter.ts`, `schemas/clawai-policy.schema.json`                                                                                        | Domain rules ship, closing the loop F049 opened: that row said domain rules waited on a network tool having domains, and 1.2.0 gave the agent one. `domainGlob` matches the host a call names, parsed with the URL parser so a fragment cannot impersonate an origin, and a call with no host never matches. Repository and command trust lists remain open: both are allow-shaped, and project policy has no allow outcome by design, so they belong in the organization policy rather than `.clawai`.                                                                                                                                                                                                                                                 |
| F054 | Managed MCP allowlists and denylists                | MISSING                               | zero `mcp` matches in `src/`, `tests/`, `docs/`, `package.json`                                                                                                                                  | MCP does not exist in the extension, so there is no policy surface to gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| F055 | Zero data retention mode                            | MISSING                               | `src/core/enterprise-policy.ts:14`, `src/services/attachment-request-service.ts:69`                                                                                                              | No retention flag, no backend capability negotiation, no local-only transcript path, no posture receipt.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

Tally: 13 SHIPPED, 6 PARTIAL, 4 MISSING, 1 CONFLICT. (Counted row-by-row, which
is the only number worth trusting here: this line was once carried forward by
adding a delta to a base that was already stale, and drifted. Recount, never
adjust.)

## Reuse map — the seam each gap must extend

- **F032** → the composer textarea plus a new inbound message in
  `chat-inbound-message.ts`; resolve candidates through
  `workspace-intelligence-service.ts` and feed them into `context-collector.ts`
  as context candidates.
- **F033** → narrowed in 0.85.0. `ContextCandidate` carries `startLine`/`endLine`;
  the tool's whole-file-hash-on-a-ranged-read staleness contract was not
  mirrored, deliberately — nothing yet consumes a hash on the context path, and
  a field with no reader is exactly the "present but not wired" defect this
  audit exists to catch. Staleness stays a named, open gap instead.
- **F035** → `process-supervisor-service.ts:52`, whose snapshot log is already
  redacted and capped; expose a read-only reference producer to the composer.
- **F036** → `src/core/browser-operation.ts:92` `browserScopeSchema`, sourcing
  `allowedOrigins` from policy instead of `runtime-studio-helpers.ts:61`.
- **F037** → the drop handler at `media/chat.js:2158` and `renderAttachments`.
- **F038/F039** → `chatAttachmentSchema.superRefine` in
  `src/core/chat-attachment.ts:117` is the single chokepoint where page range,
  resize and EXIF stripping must land, before `attachment-upload-service.ts`.
- **F040** → `src/core/model-catalog.ts:67` `contextTokens` through
  `src/core/extension-state.ts:32` to `chat-public-state.ts:168`, rendered beside
  the token chip at `media/chat.js:305`.
- **F041** → `durable-run-journal.ts:95`, whose slot already models summary,
  decisions, open questions and active task ids; the producer belongs in
  `run-journal-service.ts` and `conversation-session-service.ts:60`.
- **F042** → a variant of `send` at `chat-inbound-message.ts:72` that skips
  `ConversationSessionService.recordThread`.
- **F043** → `src/core/effort-mode.ts` and the activity renderer at
  `media/chat.js:383`, gated on the model catalog entry.
- **F044** → the composer control rail, webview-side only, with no host storage.
- **F046/F052** → `src/core/enterprise-policy.ts` is already complete and needs a
  loader plus call sites: verify on connect in
  `agent-connection-service.ts:105`, clamp in `configuration-service.ts:187`, and
  enforce beside `evaluatePolicyV2` in `runtime-policy-v2-adapter.ts:134`.
- **F047/F048** → `planning-tool-executor.ts:69` and
  `src/core/implementation-plan.ts:141`; persist the approved revision hash on
  `ChatSessionDescriptor` and check it in `session-control-service.ts:96`.
- **F049** → `src/core/policy-v2.ts:50` `projectPolicySchema`, adding rule arrays;
  `project-policy-service.ts:12` already loads and validates that file.
- **F050** → `src/core/workspace-path-policy.ts:64`, made composable with
  configuration and organization rules, and called from
  `src/core/chat-attachment.ts` to close the attachment gap.
- **F051** → `bounded-command-runner.ts:150` `runCommandSpec`, wrapping the spawn
  in a platform sandbox chosen from the declared effect in `command-spec.ts:16`.
- **F053** → `src/core/policy-v2.ts:71`, extended with trust lists loaded from the
  enterprise policy, wiring the currently dead safety rails.
- **F054** → no seam exists. The nearest analogue to copy is tool registration at
  `vscode-runtime-studio.ts:359` with capability gating at
  `vscode-runtime-target-adapter.ts:306`.
- **F055** → `enterprise-policy.ts:14` retention ceiling,
  `attachment-request-service.ts:101` whose rollback already deletes remote files,
  and `run-journal-service.ts` persistence.

## Ordering constraints found in the code

1. **F052 is the keystone for F046, F053, F054 and F055.** All four need a loaded,
   verified enterprise policy. `enterprise-policy.ts` is written and unreferenced;
   wiring it unblocks the whole permissions tail. Otherwise four features each
   grow their own organization-settings path.
2. **F046 must be resolved before F052 lands**, or the user-selectable
   `ENTERPRISE_LOCKED` option contradicts the server lock. Removing it from
   `package.json:275` and the composer is a breaking settings change and needs an
   ADR.
3. **F049 before F050 and F053.** All three want rule-shaped policy, and
   `projectPolicySchema` is where it belongs. Building a configurable deny list
   independently forks the policy model.
4. **F033 before F032.** F033 shipped in 0.85.0: `ContextCandidate` and the
   receipt now carry a range, and `path:L-L` typed directly into a message
   already resolves. F032 remains open — the autocomplete/fuzzy-matching
   mention affordance — and will emit references through this same shape.
5. **F038 and F039 share one chokepoint** and must ship as one batch, or the
   second rewrites the first.
6. **F040 before F041.** Compaction needs the capacity denominator F040 plumbs
   through to know when to trigger.
7. **F047 before F048.** There is no plan revision to restore until F047 creates
   one.
8. **F054 is greenfield.** No MCP client exists; it is a whole subsystem plus its
   policy, not a policy bolted onto an existing surface. Sequence it after F052.
9. **F051 is platform-forked** and independent of everything else.
10. **The F050 attachment gap is a present-day defect, not a parity gap.** It is
    the smallest closable item in this range and is described in
    `docs/parity/PROGRAM.md` as Batch 1.
